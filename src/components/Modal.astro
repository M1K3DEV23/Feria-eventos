---

---

<!-- Modal -->
<div class="modal" id="modal">
  <div class="modal-content">
    <h3>Confirmar registro</h3>
    <p>¿Estás seguro de que deseas registrarte en esta feria de empleo?</p>
    <div class="modal-buttons">
      <button id="confirm-btn">Confirmar</button>
      <button id="cancel-btn">Cancelar</button>
    </div>
  </div>
</div>

<style>
  /* Estilos para el modal */
  .modal {
    display: none;
    position: fixed;
    z-index: 1;
    top: 0;
    left: 0;
    width: 100%;
    height: 100vh;
    overflow: auto;
    background-color: rgba(0, 0, 0, 0.4);
  }

  .modal-content {
    background-color: #eaeaea;
    padding: 1rem;
    border: 1px solid var(--verde-500);
    width: 35%;
    position: relative;
    margin: auto;
    transform: translateY(50%);
    top: 10%;
  }
  .modal-content h3 {
    text-align: center;
    font-size: 1.8rem;
    font-weight: bold;
    color: var(--dorado-900);
  }

  .modal-content p {
    font-size: 1.2rem;
    text-align: center;
  }

  .modal-buttons {
    display: flex;
    justify-content: space-evenly;
    align-items: center;
    margin-top: 1rem;
  }

  .modal-buttons button {
    padding: 0.8rem 1.5rem;
    border-radius: 0.5rem;
    border: none;
    cursor: pointer;
  }

  #confirm-btn {
    background-color: var(--verde-500);
    color: var(--blanco-100);
    transition: background-color 0.3s ease;
  }
  #confirm-btn:hover {
    background-color: var(--verde-400);
    color: var(--blanco-100);
  }

  #cancel-btn {
    background-color: var(--rojo-500);
    color: var(--blanco-100);
  }
  #cancel-btn:hover {
    background-color: var(--rojo-400);
    color: var(--blanco-100);
  }

  @keyframes entrarModal {
    from {
      opacity: 0;
      transform: translateY(-10%);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @keyframes salirModal {
    from {
      opacity: 1;
      transform: translateY(0);
    }
    to {
      opacity: 0;
      transform: translateY(-10%);
    }
  }

  /* Transicion para mostrar el modal */
  .modal-entrar {
    animation: entrarModal 0.3s ease-out forwards;
  }

  /* Transicion para ocultar el modal */
  .modal-salir {
    animation: salirModal 0.3s ease-int forwards;
  }

  /* Estilos para mobiles */
  @media (max-width: 768px) {
    .modal-content {
      width: 80%;
      padding: 1.1rem;
    }

    .modal-buttons {
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      gap: 1.2rem;
    }

    .modal-buttons button {
      width: 100%;
    }
  }
</style>

<script>
  const modal = document.getElementById('modal') as HTMLDivElement;
  const confirmBtn = document.getElementById('confirm-btn') as HTMLButtonElement;
  const cancelBtn = document.getElementById('cancel-btn') as HTMLButtonElement;
  const registerBtns = document.querySelectorAll('.register-btn') as NodeListOf<HTMLButtonElement>;

  registerBtns.forEach((btn) => {
    btn.addEventListener('click', () => {
      modal.classList.add('modal-entrar');
      modal.style.display = 'block';
    });
  });

  confirmBtn.addEventListener('click', () => {
    // Aquí puedes agregar la lógica para redirigir al usuario a la página de registro
    window.location.href = '/AttedendanceBadge'; // Reemplaza con la ruta correcta
  });

  cancelBtn.addEventListener('click', () => {
    modal.classList.add('modal-salir');

    // Esperar a que termine la animación de salida para cerrar el modal
    setTimeout(() => {
      modal.style.display = 'none';
      modal.classList.remove('modal-entrar', 'modal-salir');
    }, 250); // 300 milisegundos es la duracion de la animacion
  });

  window.addEventListener('click', (event) => {
    if (event.target === modal) {
      modal.style.display = 'none';
    }
  });
</script>
